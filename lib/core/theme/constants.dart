import 'package:flutter/material.dart';

const kDefaultPadding = EdgeInsets.symmetric(horizontal: 20, vertical: 25);
const kHorizontalPadding = EdgeInsets.symmetric(horizontal: 20);
const kVerticalPadding = EdgeInsets.symmetric(vertical: 25);
const kHorizontalPadding2 = EdgeInsets.symmetric(horizontal: 12);
const kPaddingButtom = EdgeInsets.symmetric(horizontal: 64, vertical: 8);