import 'package:flutter/material.dart';

const kDefaultPadding = EdgeInsets.fromLTRB(20, 20, 20, 20);
const kBalancePadding = EdgeInsets.symmetric(horizontal: 20, vertical: 5);
const kHorizontalPadding2 = EdgeInsets.symmetric(horizontal: 12);
const kVerticalPadding = EdgeInsets.symmetric(vertical: 25);
const kPaddingButton = EdgeInsets.symmetric(horizontal: 64, vertical: 8);
